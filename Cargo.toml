[package]
name = "reuseport"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
axum = "0.8.4"
anyhow = "1"
socket2 = { version = "0.6", features = ["all"] }